import { Button } from "std-widgets.slint";

export component MainWindow inherits Window {
    title: "Waitaha Recovery";
    background: black;

    Text {
        text: "Hello world!";
        color: white;
    }
}
